3️⃣ 运行 特征聚合
python manage.py build_features
聚类运行  python manage.py run_dbscan
python manage.py run_dbscan --eps 0.9 --min_samples 4


⭐ 如何运行（无需前端）

进入 Django shell：

py manage.py shell


运行：

from analysis.services.apriori_service import apriori
import pprint

result = apriori(min_support=0.05, min_confidence=0.4)  # 你可以调参数
pprint.pprint(result)

你可以这样运行：
python manage.py shell

from analysis.dbscan_cluster import run_dbscan_all
run_dbscan_all()

⚙️ 2. 如何运行

你可以在 Django shell 或管理后台运行：

方式 1：命令行执行
py manage.py shell

from analysis.tagging import run_player_tagging
run_player_tagging()

然后运行：

python manage.py run_tags


一、数据库准备
1️⃣ TagCategory 表

确保已经有行为维度（类别）：

code	name
daily_task	日常活跃度
purchase	付费行为
explore	探索行为

操作：

from analysis.models import TagCategory

# 新建类别（如果未创建）
TagCategory.objects.get_or_create(code="daily_task", defaults={"name": "日常活跃度"})
TagCategory.objects.get_or_create(code="purchase", defaults={"name": "付费行为"})
TagCategory.objects.get_or_create(code="explore", defaults={"name": "探索行为"})
TagCategory.objects.get_or_create(code="gacha", defaults={"name": "抽卡行为"})
TagCategory.objects.get_or_create(code="event_task", defaults={"name": "活动活跃度"})
TagCategory.objects.get_or_create(code="material_farm", defaults={"name": "刷材料行为"})

2️⃣ Tag 表

给每个类别划分标签区间，确保覆盖全部范围：

daily_task
name	min	max
咸鱼玩家	0.0	0.2
普通玩家	0.2	0.5
活跃玩家	0.5	1.0
purchase
name	min	max
0氪玩家	0.0	0.001
少氪玩家	0.001	0.01
高氪玩家	0.01	10.0
explore
name	min	max
探索少	0.0	0.2
探索中	0.2	0.5
探索多	0.5	1.0

操作示例（Python shell）：

from analysis.models import Tag, TagCategory

daily = TagCategory.objects.get(code="daily_task")
purchase = TagCategory.objects.get(code="purchase")
explore = TagCategory.objects.get(code="explore")
gacha = TagCategory.objects.get(code="gacha")
event_task = TagCategory.objects.get(code="event_task")
material_farm = TagCategory.objects.get(code="material_farm")
Tag.objects.get_or_create(category=daily, name="咸鱼玩家", min_value=0.0, max_value=0.05)
Tag.objects.get_or_create(category=daily, name="普通玩家", min_value=0.05, max_value=0.1)
Tag.objects.get_or_create(category=daily, name="活跃玩家", min_value=0.1, max_value=1.0)
Tag.objects.get_or_create(category=purchase, name="0氪玩家", min_value=0.0, max_value=0.005)
Tag.objects.get_or_create(category=purchase, name="轻氪玩家", min_value=0.005, max_value=0.01)
Tag.objects.get_or_create(category=purchase, name="重氪玩家", min_value=0.01, max_value=1.0)
Tag.objects.get_or_create(category=explore, name="不爱探索玩家", min_value=0.0, max_value=0.12)
Tag.objects.get_or_create(category=explore, name="适中探索玩家", min_value=0.12, max_value=0.2)
Tag.objects.get_or_create(category=explore, name="满探索玩家", min_value=0.2, max_value=1.0)
Tag.objects.get_or_create(category=gacha, name="屯屯鼠玩家", min_value=0.0, max_value=0.01)
Tag.objects.get_or_create(category=gacha, name="爱抽卡玩家", min_value=0.01, max_value=0.015)
Tag.objects.get_or_create(category=gacha, name="平铺玩家", min_value=0.015, max_value=1.0)
Tag.objects.get_or_create(category=event_task, name="低参与玩家", min_value=0.0, max_value=0.05)
Tag.objects.get_or_create(category=event_task, name="中参与玩家", min_value=0.05, max_value=0.1)
Tag.objects.get_or_create(category=event_task, name="重参与玩家", min_value=0.1, max_value=1.0)
Tag.objects.get_or_create(category=material_farm, name="少量刷材料玩家", min_value=0.0, max_value=0.6)
Tag.objects.get_or_create(category=material_farm, name="适中刷材料玩家", min_value=0.6, max_value=0.7)
Tag.objects.get_or_create(category=material_farm, name="锄大地玩家", min_value=0.7, max_value=1.0)


同理创建 purchase、explore 下的标签。

✅ 注意：标签区间必须覆盖玩家所有可能值，否则会打不上标签。

四、完整启动顺序（一定照这个）
① 启动 Redis（给 Celery 用）
redis-server

② 启动 Kafka 进入/bin/windows
.\zookeeper-server-start.bat ..\..\config\zookeeper.properties
.\kafka-server-start.bat ..\..\config\server.properties

③ 启动 Celery Worker（吃 Redis 队列）
celery -A analysis worker -l info -P solo
⚠️ 必须同时启动
celery -A analysis worker -l info
celery -A analysis beat -l info

④ 启动 Kafka Consumer（桥梁）
python analysis/kafka/consumer.py

⑤ 启动 Django
python manage.py runserver

from pyflink.datastream.connectors import FlinkKafkaConsumer, FlinkKafkaProducer
from pyflink.common.serialization import SimpleStringSchema
from pyflink.datastream.functions import MapFunction, KeyedProcessFunction
from pyflink.common.state import ValueStateDescriptor